from app.trading.gold.data.data_loader import data_loader
def run(p): d=data_loader.load(p); return {'candles':len(d),'first':d[0],'last':d[-1]}
def sl_hit(direction,sl,d):
 return d["Low"]<=sl if direction=="BUY" else d["High"]>=sl
